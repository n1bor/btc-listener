[package]
name = "btc-listener-kv"
version = "0.1.0"
edition = "2021"
description = "A RocksDB behind the btc-listener Kv capability contract."

[lib]
name = "btc_listener_kv"

[dependencies]
aver-rt = { path = "/home/owensr/aver/aver/aver-rt" }
rocksdb = { version = "0.25", default-features = false, features = ["lz4"] }

[dev-dependencies]
tempfile = "3"
