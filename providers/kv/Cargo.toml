[package]
name = "btc-listener-kv"
version = "0.1.0"
edition = "2021"
description = "A LevelDB behind the btc-listener Kv capability contract."

[lib]
name = "btc_listener_kv"

[dependencies]
aver-rt = { path = "/home/owensr/aver/aver/aver-rt", version = "=0.4.10" }
rusty-leveldb = "4.0"

[dev-dependencies]
tempfile = "3"
